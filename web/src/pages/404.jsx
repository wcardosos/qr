import * as React from "react"

const NotFoundPage = () => {
  return (
    <main>
      <p>The page that you try to access does not exists</p>
    </main>
  )
}

export default NotFoundPage

export const Head = () => <title>Not found</title>
